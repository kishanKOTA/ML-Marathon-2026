# Experiment log

One row per run. Large artifacts (checkpoints, prediction dumps) go in `results/artifacts/` (gitignored), not here.

| Date | Who | Model / config | Metric(s) | Notes |
|---|---|---|---|---|
